# JusticeGuide Web (Next.js)

This app is the frontend for the Python API in the project root.

## Local development

```bash
cd apps/web
npm install
npm run dev
```

Set API URL if required:

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```
